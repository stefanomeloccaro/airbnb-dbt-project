snapshots:
  - name: scd_stg_neighbourhoods
    description: "SCD Type 2 snapshot to track changes in neighbourhoods attributes over time"
    relation: ref('stg_neighbourhoods')
    config:
      schema: SNAPSHOTS
      unique_key: NEIGHBOURHOOD_ID
      strategy: check
      check_cols: 'all'
      hard_deletes: invalidate