snapshots:
  - name: scd_stg_reviews
    description: "SCD Type 2 snapshot to track changes in review attributes over time"
    relation: ref('stg_reviews')
    config:
      schema: SNAPSHOTS
      unique_key: review_id
      strategy: check
      check_cols: 'all'
      hard_deletes: invalidate